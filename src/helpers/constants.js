export const HOME_PAGE_NAME = 'HomePage';
export const DEFAULT_TOTAL_VISIBLE_ITEM = 10; // default value for the amount of item will be visible in pagination
export const DEFAULT_ITEM_PER_PAGE = 10; // default for limit
export const WIDTH_ITEM_PAGINATION = 30;
export const randomId = () => Math.random()
        .toString(36)
        .substr(2, 10);

export const Regex = {
    NUMBER_ONLY: '^[0-9]+$',
};
